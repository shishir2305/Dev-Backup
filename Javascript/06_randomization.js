// generating a random number

var num = Math.random();
var res = Math.floor(num * 10);
console.log(res);

// it will generate a random number between 0 and 10 with 10 exclusive
// Math.floor(num * 10) will round the number to the nearest integer
